# RELEASE

## Checklist
- Update `docs/CHANGELOG.md` with shipped changes.
- Run `make check` and ensure CI is green.
- Tag `vX.Y.Z` and create a GitHub Release.
- Update `projects.md` with status + release link.
